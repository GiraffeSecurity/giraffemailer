# Copy to .env before: docker compose up
#   cp .env.example .env
#   echo "GM_SECRET_KEY=$(openssl rand -hex 32)" >> .env
#
# Or one line:
#   echo "GM_SECRET_KEY=$(openssl rand -hex 32)" > .env

GM_SECRET_KEY=
